import { Defaults } from './defaults.enum';
import { OnInit } from '@angular/core';
export declare class NgxContentLoadingComponent implements OnInit {
    width: Defaults;
    height: Defaults;
    primaryColor: string;
    secondaryColor: string;
    speed: string;
    preserveAspectRatio: Defaults;
    readonly viewBox: string;
    ngOnInit(): void;
}
